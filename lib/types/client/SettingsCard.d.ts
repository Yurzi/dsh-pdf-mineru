import { type SettingsPageProps } from './SettingsPage.js';
/** Native disclosure keeps the form mounted, including unsaved edits, when closed. */
export declare function SettingsCard(props: SettingsPageProps): JSX.Element;
